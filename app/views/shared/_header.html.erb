<nav class="navbar navbar-default navbar-statis-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-brand-centered">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand"><%= link_to image_tag(asset_path('logo-navbar.png')), root_path %></div>
    </div>

    <div class="collapse navbar-collapse" id="navbar-brand-centered">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Suscríbete', add_subscription_orders_path, method: :post %></li>
        <li><%= link_to 'Conoce nuestras variedades', origenes_path %></li>
        <li><%= link_to 'Tienda', store_path %></li>

      </ul>
      <ul class="nav navbar-nav navbar-right actions">
        <% if current_user %>
          <p class="brown">
            ¡Hola, <%= current_user.first_name %>!
            <% if current_user.active_subscription %>
              | <%= link_to 'Mi suscripción', subscription_path %>
            <% end %>
            <% if current_order && current_order.items.any? %>
              | <%= link_to raw("<span class='glyphicon glyphicon-shopping-cart'></span> #{current_order.items.size}"), cart_orders_path %>
            <% end %>
            | <%= link_to 'Cerrar sesión', signout_path, method: :delete %>
          </p>
        <% else %>
          <p class="brown">
            <%= link_to 'Inicia sesión', signin_path %>
            <% if current_order && current_order.items.any? %>
              | <%= link_to raw("<span class='glyphicon glyphicon-shopping-cart'></span> #{current_order.items.size}"), cart_orders_path %>
            <% end %>
          </p>
        <% end %>         
      </ul>
    </div>
  </div>
</nav>