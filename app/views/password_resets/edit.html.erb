<div class="row">
  <div class="col-md-12">
  <%= render 'shared/flash_messages' %>
    <div class="page-header">
      <h2>Ingresa tu nueva contraseña</h2>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-4">
    <%= bootstrap_form_for :password_reset, url: password_reset_path, method: :patch do |f| %>
      <%= f.password_field :password, label: 'Nueva contraseña', required: true %>
      <%= f.submit 'Actualizar contraseña', class: 'btn btn-primary', data: { disable_with: 'Actualizar contraseña' } %>
    <% end %>
  </div>
</div>